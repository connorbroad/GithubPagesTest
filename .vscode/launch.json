{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run (yarn start)",
      "type": "node-terminal",
      "request": "launch",
      "command": "yarn start",
      "cwd": "${workspaceFolder}"
    }
  ]
}